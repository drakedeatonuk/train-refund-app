import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'mydelayrepay',
  webDir: '../../../dist/apps/mdr/mob/www',
  bundledWebRuntime: false,
};

export default config;
