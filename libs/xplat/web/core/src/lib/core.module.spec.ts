describe('Core module generic test', () => {
  it('Should be true', () => {
    expect(true).toBeTruthy();
  });
});

/*
import { MultiCoreModule } from './core.module';

describe('MultiCoreModule', () => {
  it('should work', () => {
    expect(new MultiCoreModule(null)).toBeDefined();
  });
});
*/
